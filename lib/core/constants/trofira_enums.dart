enum AppStatus {
  uninitialized,
  onboarding,
  unauthenticated,
  authenticated,
}

enum VendorStatus {
  loading,
  uninitialized,
  initialized,
}
enum DriverStatus {
  loading,
  uninitialized,
  initialized,
}
