import 'package:flutter/material.dart';

class  DriversAccount extends StatelessWidget {
  const  DriversAccount({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Drivers Account"),
    );
  }
}